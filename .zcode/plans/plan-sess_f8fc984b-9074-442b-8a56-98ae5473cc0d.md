# План: приведение README/USER_GUIDE/manifest к рекомендациям SillyTavern

Сверка с официальным гайдом (docs.sillytavern.app/for-contributors/writing-extensions/) показала: код и манифест в основном соответствуют рекомендациям (обязательные поля заполнены, хуки экспортируются, `renderExtensionTemplateAsync` + `data-i18n`, спокойная консоль, `getContext()`). Главное расхождение — README не содержит трёх обязательных разделов: **installation instructions, usage examples, list of features**.

## 1. README.md — переписать на английском по официальной структуре

Решение пользователя: EN README + RU гайд. Все существующие ценные разделы сохраняются, но переводятся и перестраиваются:

- **Title + краткое описание** — что делает расширение (лента Discord-стиль комментариев через отдельный LLM-запрос).
- **Features** — явный список фич (сейчас его нет): лента с генерацией отдельным запросом; кэш на каждое сообщение/свайп с restore; два режима хранения; изоляция checkpoint/branch; Connection Manager профили + кастомный endpoint; редактируемые «вайб»-шаблоны; звуки уведомлений (серверное хранение); темы/жесты; экспорт/импорт; диагностический дамп; `/dscomments`; i18n.
- **Installation** (новое): требование ST ≥ 1.18.0; установка через Extensions → Install extension (место под git-URL репозитория — плейсхолдер, т.к. репозитория пока нет); ручная установка копированием в `data/<user>/extensions/`; проверка появления в Manage Extensions.
- **Usage** (новое): как открыть ленту (launcher), генерация, поведение при свайпе/смене сообщения, слэш-команды, экспорт/импорт; ссылка на USER_GUIDE.md для русскоязычных пользователей.
- **Storage modes / Checkpoints** — сжатый перевод текущих разделов.
- **Notification sounds, Diagnostics, Compatibility notes** — перевод; сохранить все три заметки совместимости (minimum_client_version, `saveMetadata()` без debounce, `BASE_URL` без слэша).
- **Development** — перевод; исправить дрейф: раздел про `.st-verify/` переписать (снапшот генерируется по требованию и не коммитится — папки в репо нет).
- **Packaging** — перевод; добавить `USER_GUIDE.md` в состав дистрибутива (его там нет, а для RU-пользователей это единственный гайд).
- **i18n, Project structure** — перевод.

## 2. USER_GUIDE.md — дополнить (остаётся русским)

- Добавить раздел **«Установка»** (требования + шаги, синхронно с README).
- Добавить раздел **«Использование»** — короткий walkthrough: launcher, генерация, поведение при свайпе, слэш-команды.
- Добавить ссылку на README.md для деталей разработки.
- Остальные разделы (режимы хранения, экспорт/импорт, диагностика, ограничения) не трогать.

## 3. manifest.json — без правок, решения зафиксировать

- `homePage: ""` — остаётся пустым (решение пользователя; заполнить при появлении репозитория).
- `author: "DS Comments"` — оставить; заменить на реальное имя/контакт при публикации.
- `auto_update: false` — оставить: расширение жёстко привязано к 1.18.0 и не должно молча обновляться; переключить на `true` при публикации.

## 4. Удалить устаревший дубль `i18n/en-us.json` в корне

Файл (2.7 КБ) разошёлся с актуальным `src/i18n/en-us.json` (11.3 КБ), на который указывает манифест; ссылок на корневой путь нет нигде (проверено grep'ом по index.js, manifest.json, settings.html, src/).

## Вне скоупа (зафиксировать как будущие пункты)

- Монтаж настроек в `#extensions_settings` вместо рекомендуемого `#extensions_settings2` — менять только с проверкой на целевой версии ST, в этот раз не трогаем.

Порядок работ: README → USER_GUIDE → удаление дубля → финальная вычитка на расхождения с фактами из кода.